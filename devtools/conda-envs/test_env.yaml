name: smirnoff-plugins-test
channels:
  - conda-forge

dependencies:
    # Base depends
  - python

    # Core dependencies
  - numpy
  - openmm
  - openff-toolkit =0.16
  - openff-interchange ~=0.3.25
  - openff-utilities
  - openff-models

    # Testing
  - pytest
  - pytest-cov
  - pytest-xdist
  - pytest-randomly

  - mypy

  - pip:
    - git+https://github.com/openforcefield/openff-interchange.git@virtual-site-plugins
