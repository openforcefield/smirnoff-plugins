name: smirnoff-plugins-test
channels:
  - conda-forge
  - conda-forge/label/openff-interchange_rc

dependencies:
    # Base depends
  - python
  - pip

    # Core dependencies
  - numpy
  - openmm
  - openff-toolkit >=0.12.1
  - openff-interchange >=0.3.0beta1
  - openff-utilities
  - openff-models

    # Testing
  - pytest
  - pytest-cov
  - codecov
  - pip:
    - git+https://github.com/openforcefield/openff-interchange.git@plugin-fixes
