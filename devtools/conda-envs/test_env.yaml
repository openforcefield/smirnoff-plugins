name: smirnoff-plugins-test
channels:
  - jaimergp/label/unsupported-cudatoolkit-shim
  - conda-forge

dependencies:
    # Base depends
  - python

    # Core dependencies
  - numpy
  - openmm
  - openff-toolkit =0.14.0
  - openff-utilities
  - openff-models

    # Testing
  - pytest
  - pytest-cov
  - pytest-xdist

  - mypy =1.2
