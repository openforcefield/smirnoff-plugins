name: smirnoff-plugins-test
channels:
  - conda-forge
dependencies:
    # Base depends
  - python

    # Core dependencies
  - numpy
  - openmm
  - openff-toolkit =0.16
  - openff-interchange ~=0.4.0
  - openff-utilities
  - rdkit

    # Testing
  - pytest
  - pytest-cov
  - pytest-xdist
  - pytest-randomly

  - mypy
