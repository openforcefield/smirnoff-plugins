name: smirnoff-plugins-test
channels:
  - conda-forge
dependencies:
    # Base depends
  - python

    # Core dependencies
  - numpy <2.4
  - openmm
  - openff-toolkit >=0.17
  - openff-interchange ~=0.4.6
  - openff-utilities
  - rdkit

    # Testing
  - pytest
  - pytest-cov
  - pytest-xdist
  - pytest-randomly

  - mypy
