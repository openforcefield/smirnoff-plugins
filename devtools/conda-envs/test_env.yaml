name: smirnoff-plugins-test
channels:
  - conda-forge

dependencies:
    # Base depends
  - python

    # Core dependencies
  - numpy
  - openmm
  - openff-toolkit =0.15
  - openff-interchange
  - openff-utilities
  - openff-models

    # Testing
  - pytest
  - pytest-cov
  - pytest-xdist
  - pytest-randomly

  - mypy

  - pip:
    - git+https://github.com/openforcefield/openff-toolkit.git@base-virtual-site-type
    - git+https://github.com/openforcefield/openff-interchange.git@base-virtual-site-type
