name: smirnoff-plugins-test
channels:
  - conda-forge

dependencies:
    # Base depends
  - python

    # Core dependencies
  - numpy
  - openmm
  - openff-toolkit =0.16
  - openff-interchange
  - openff-utilities
  - openff-models

    # Testing
  - pytest
  - pytest-cov
  - pytest-xdist
  - pytest-randomly

  - mypy
