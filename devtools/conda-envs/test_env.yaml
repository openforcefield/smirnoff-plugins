name: test
channels:
  - conda-forge

dependencies:
    # Base depends
  - python
  - pip

    # Core dependencies
  - openff-toolkit
  - packaging  # Missing OFF TK dependency.
  - openmm

    # Testing
  - pytest
  - pytest-cov
  - codecov
